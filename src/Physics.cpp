void PhysicsLoop() {
    for (int i = 0; i < sizeof(currentScene.colliderHitboxes); i++) {
        // for (int j = 0; j < sizeof(currentScene.colliderHitboxes); j++) {
        // std::cout << "colliderId: " << currentScene.colliderHitboxes[i].objectId << std::endl;
        // }
    }
}

