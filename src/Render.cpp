BoxCollider colliderObjects[2];

void MoveColliderObject(BoxCollider object, sf::Vector2f dir) {
    
}

void Render(sf::RenderWindow& window) {
    sf::CircleShape myshape = sf::CircleShape(50.0);
    window.draw(myshape);
}